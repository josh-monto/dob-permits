name: 'dob_permits'
version: '1.0.0'

profile: 'dob_permits'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dob_permits:
    staging:
      +materialized: view
    marts:
      +materialized: incremental
